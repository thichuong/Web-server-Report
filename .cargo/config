# Cargo configuration for optimized development and build

[env]
# Environment variables for runtime optimization
RUST_LOG = { value = "info", force = false }
TOKIO_THREAD_STACK_SIZE = { value = "4194304", force = false }  # 4MB per thread
RAYON_NUM_THREADS = { value = "0", force = false }  # Auto-detect cores

[profile.dev.package."*"]
# Optimize dependencies even in dev mode for better runtime performance
opt-level = 2
