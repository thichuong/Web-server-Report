# Environment Configuration for Railway Deployment
# Copy this to .env and fill in your actual values

DATABASE_URL=postgresql://your_db_url
AUTO_UPDATE_SECRET_KEY=your_secret_key
HOST=0.0.0.0
PORT=8000
TAAPI_SECRET=your_taapi_jwt_token
REDIS_URL=redis://your_redis_url
FINNHUB_API_KEY=your_finnhub_key

# CoinMarketCap API Configuration (Optional - for fallback support)
# Get your API key from: https://pro.coinmarketcap.com/
# If not provided, system will only use CoinGecko (free API)
CMC_API_KEY=your_coinmarketcap_api_key_here

# WebSocket Service URL Configuration
# URL of the separate WebSocket service for real-time updates
# Development: ws://localhost:8081
# Production: wss://your-websocket-domain.com
WEBSOCKET_SERVICE_URL=ws://localhost:8081

# Logging Configuration
# Development: info (shows important events + warnings + errors)
# Production: warn (only warnings and errors, less verbose)
# Options: error, warn, info, debug, trace
RUST_LOG=info
