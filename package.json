{
  "name": "web-server-report",
  "version": "1.0.0",
  "description": "Crypto Market Dashboard - Frontend Build Pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:prod": "node build.js --production",
    "build:watch": "node build.js --watch",
    "clean": "rm -rf dist/",
    "prebuild": "npm run clean"
  },
  "devDependencies": {
    "esbuild": "^0.19.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "crypto",
    "dashboard",
    "websocket",
    "real-time",
    "market-data"
  ],
  "author": "thichuong",
  "license": "MIT"
}
